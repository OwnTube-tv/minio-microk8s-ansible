---

- name: Reload systemd daemon
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Restart MinIO service
  ansible.builtin.systemd:
    name: minio
    state: restarted
  check_mode: no

...
